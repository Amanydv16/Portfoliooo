import Scene from "./Scene";

const CharacterModel = () => {
  // Set to true to use the static 2D image, set to false to use the interactive 3D model
  const useStaticImage = false;
  return <Scene />;
};

export default CharacterModel;
